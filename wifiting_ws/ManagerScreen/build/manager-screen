#!/usr/bin/env bash
set -e

if [[ ! -f /opt/ros/jazzy/setup.bash ]]; then
    echo "ROS 2 Jazzy was not found at /opt/ros/jazzy" >&2
    exit 1
fi

source /opt/ros/jazzy/setup.bash

launcher_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "${launcher_dir}/ManagerScreen" "$@"
